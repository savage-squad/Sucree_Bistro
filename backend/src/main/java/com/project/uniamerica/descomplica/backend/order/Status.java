package com.project.uniamerica.descomplica.backend.order;

public enum Status {
    pendente("pendente"),
    aceite("aceite"),
    concluido("concluido"),
    cancelado("cancelado");

    Status(String valor) {
    }
}
